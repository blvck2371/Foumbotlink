/// Palette Foumbot Link : rouge · blanc · noir
library;

import 'package:flutter/material.dart';

class AppColors {
  static const black = Color(0xFF0A0A0A);
  static const blackSoft = Color(0xFF161616);
  static const blackElevated = Color(0xFF1F1F1F);
  static const white = Color(0xFFFFFFFF);
  static const whiteSoft = Color(0xFFF5F5F5);
  static const whiteMuted = Color(0xFFB3B3B3);
  static const gray = Color(0xFF6B6B6B);
  static const red = Color(0xFFE53935);
  static const redDeep = Color(0xFFC62828);
  static const redSoft = Color(0xFFFF6B66);
  /// Accent secondaire (onboarding) — l’UI principale reste rouge · blanc · noir
  static const blue = Color(0xFF1E88E5);
}
